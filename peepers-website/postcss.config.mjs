/** PostCSS configuration using the @tailwindcss/postcss adapter (required for Tailwind v4+) */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};

export default config;
